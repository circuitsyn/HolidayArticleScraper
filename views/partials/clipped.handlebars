Clipped Page

  <div class="container">
    <div class="row col-12">
  
          {{!-- Injecting card blocks --}}
            {{#each result}}
                {{> clippedDir/clipped-block }}
            {{/each}}  
    </div>
    <a href="/">← Back To Home</a>
  </div>